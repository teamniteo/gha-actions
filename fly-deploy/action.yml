name: "Fly Deploy"
description:
  Build the image with uv and Nix, push it to the app's registry on Fly
  and deploy it. Needs flyctl, skopeo, uv and nix-build, so run the nix
  action first.

inputs:
  app:
    description: Fly app name.
    required: true
  token:
    description: Fly API token that can deploy the app, and create it when org is given.
    required: true
  org:
    description: Create the app in this org when it does not exist yet. Leave empty for an app that must already exist.
    default:
  sha:
    description: Commit the image is built from. Tags the image and reaches the app as GIT_COMMIT.
    default: ${{ github.event.pull_request.head.sha || github.event.workflow_run.head_sha || github.sha }}
  secrets:
    description: KEY=VALUE lines to stage as app secrets before the deploy.
    default:
  project:
    description: Directory holding the pyproject.toml and uv.lock the image's venv is built from.
    default: .
  prebuild:
    description: Command to run before the venv is built, such as compiling a frontend.
    default:

outputs:
  url:
    description: The app's fly.dev URL.
    value: https://${{ inputs.app }}.fly.dev

runs:
  using: "composite"
  steps:
    - name: Prepare the app
      if: ${{ inputs.prebuild != '' }}
      shell: bash
      run: ${{ inputs.prebuild }}

    - name: Build the image
      shell: bash
      env:
        PROJECT: ${{ inputs.project }}
      run: |
        venv=$(mktemp -d)/venv
        uv venv --project "$PROJECT" --relocatable "$venv"
        UV_PROJECT_ENVIRONMENT="$venv" uv sync --project "$PROJECT" --frozen --no-dev --no-editable
        nix-build -A image --arg venv "$venv"

    - name: Create the app when it does not exist
      if: ${{ inputs.org != '' }}
      shell: bash
      env:
        APP: ${{ inputs.app }}
        FLY_API_TOKEN: ${{ inputs.token }}
        ORG: ${{ inputs.org }}
      run: flyctl status -a "$APP" > /dev/null 2>&1 || flyctl apps create "$APP" --org "$ORG"

    - name: Push the image to the app's registry
      shell: bash
      env:
        APP: ${{ inputs.app }}
        FLY_API_TOKEN: ${{ inputs.token }}
        SHA: ${{ inputs.sha }}
      run: |
        ./result > "$RUNNER_TEMP/image.tar"
        skopeo --insecure-policy copy --retry-times 3 --dest-creds "x:$FLY_API_TOKEN" "docker-archive:$RUNNER_TEMP/image.tar" "docker://registry.fly.io/$APP:$SHA"

    - name: Stage the secrets
      if: ${{ inputs.secrets != '' }}
      shell: bash
      env:
        APP: ${{ inputs.app }}
        FLY_API_TOKEN: ${{ inputs.token }}
        SECRETS: ${{ inputs.secrets }}
      run: flyctl secrets import -a "$APP" --stage <<< "$SECRETS"

    # Scheduled Machines, made with `flyctl machine run --schedule`, live in
    # the app next to the web ones. A job caught running gets ten minutes to
    # finish while the app still serves; then only the web Machines stop.
    - name: Stop the app
      shell: bash
      env:
        APP: ${{ inputs.app }}
        FLY_API_TOKEN: ${{ inputs.token }}
      run: |
        machines=$(flyctl machine list -a "$APP" --json)
        while read -r id name; do
          echo "Waiting for scheduled Machine $name to finish its job"
          flyctl machine wait "$id" -a "$APP" --state stopped --wait-timeout 10m \
            || echo "::warning::Scheduled Machine $name still running after 10m, the deploy cuts its job short"
        done < <(jq -r '.[] | select(.config.schedule != null and .state != "stopped") | "\(.id) \(.name)"' <<< "$machines")
        machines=$(jq '[.[] | select(.config.schedule == null)]' <<< "$machines")
        for id in $(jq -r '.[].id' <<< "$machines"); do
          flyctl machine cordon "$id" -a "$APP"
        done
        while read -r id state; do
          if [[ "$state" != stopped ]]; then
            flyctl machine stop "$id" -a "$APP" --wait-timeout 1m
          fi
        done < <(jq -r '.[] | "\(.id) \(.state)"' <<< "$machines")

    - name: Deploy
      shell: bash
      env:
        APP: ${{ inputs.app }}
        FLY_API_TOKEN: ${{ inputs.token }}
        SHA: ${{ inputs.sha }}
      run: |
        config=fly.toml
        if [[ -d backend ]]; then
          config=backend/fly.toml
        fi
        flyctl deploy --config "$config" -a "$APP" --image "registry.fly.io/$APP:$SHA" --ha=false -e "GIT_COMMIT=$SHA" -e "DEPLOYED_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)"

    # `flyctl deploy` never updates a scheduled Machine, so move every one of
    # them to the image that just went live, so none runs old code against a
    # schema the release command may have migrated. They are stopped since the
    # first step, unless a schedule fired meanwhile; that job is cut short.
    - name: Roll the scheduled Machines onto the new image
      shell: bash
      env:
        APP: ${{ inputs.app }}
        FLY_API_TOKEN: ${{ inputs.token }}
        SHA: ${{ inputs.sha }}
      run: |
        while read -r id state name; do
          if [[ "$state" != stopped ]]; then
            echo "::warning::Scheduled Machine $name started during the deploy, the update cuts its job short"
          fi
          flyctl machine update "$id" -a "$APP" --image "registry.fly.io/$APP:$SHA" --skip-start --yes
        done < <(flyctl machine list -a "$APP" --json | jq -r '.[] | select(.config.schedule != null) | "\(.id) \(.state) \(.name)"')

    - name: Start the app
      shell: bash
      env:
        APP: ${{ inputs.app }}
        FLY_API_TOKEN: ${{ inputs.token }}
      run: |
        machines=$(flyctl machine list -a "$APP" --json | jq '[.[] | select(.config.schedule == null)]')
        while read -r id state; do
          if [[ "$state" != started ]]; then
            flyctl machine start "$id" -a "$APP"
          fi
          flyctl machine wait "$id" -a "$APP" --state started --wait-timeout 1m
        done < <(jq -r '.[] | "\(.id) \(.state)"' <<< "$machines")
        for id in $(jq -r '.[].id' <<< "$machines"); do
          flyctl machine uncordon "$id" -a "$APP"
        done

    - name: Check the app
      shell: bash
      env:
        APP: ${{ inputs.app }}
      run: |
        echo "Waiting for the app to become ready ..."
        deadline=$((SECONDS + 60))
        for attempt in {0..12}; do
          if errors=$(curl --fail --silent --show-error --output /dev/null --max-time 10 "https://$APP.fly.dev" 2>&1); then
            echo "The app is ready."
            exit 0
          fi
          if (( attempt == 12 || SECONDS + 3 >= deadline )); then
            break
          fi
          echo "Retrying ..."
          sleep 3
        done
        echo "::error::The app did not become ready after deployment."
        printf '%s\n' "$errors"
        exit 1
